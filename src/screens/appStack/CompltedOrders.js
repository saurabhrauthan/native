import { View, Text, StyleSheet } from "react-native";
import React from "react";

const CompltedOrders = () => {
  return (
    <View style={styles.container}>
      <Text>jjs</Text>
    </View>
  );
};

export default CompltedOrders;

const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: "center",
    justifyContent: "center",
  },
});
